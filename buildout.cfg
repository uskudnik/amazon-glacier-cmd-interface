[buildout]
parts = 
	bpython
develop = .
extensions = mr.developer
eggs = 
	glacier
	boto

[sources]
boto = git https://github.com/boto/boto.git branch=glacier update=true

[bpython]
recipe = zc.recipe.egg:scripts
eggs = 
	bpython
	${buildout:eggs}